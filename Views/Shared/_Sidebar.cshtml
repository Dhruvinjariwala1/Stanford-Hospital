@* @using Microsoft.AspNetCore.Identity
@inject UserManager<ApplicationUser> UserManager
@{
    var fullName = @UserManager.GetUserAsync(User).Result.FirstName + " " + @UserManager.GetUserAsync(User).Result.LastName;
    var userimg = @UserManager.GetUserAsync(User).Result.Image;
} *@

<!-- Main Sidebar Container -->
<aside class="main-sidebar sidebar-dark-primary elevation-4">
    <!-- Brand Logo -->
    <a asp-action="Index" asp-controller="Home" class="brand-link">
        <img src="~/dist/img/medical-team.png" alt="medical-team" class="brand-image img-circle elevation-3" style="opacity: .8">
        <span class="brand-text font-weight-light">Stanford Hospital</span>
    </a>

    <!-- Sidebar -->
    <div class="sidebar">
        <!-- Sidebar user panel (optional) -->
        @* <div class="user-panel mt-3 pb-3 mb-3 d-flex">
            <div class="nav-item"> *@
                @* <a asp-controller="User" asp-action="EditProfile" class="nav-link @ViewBag.isusereditprofile">
                   @if(!string.IsNullOrWhiteSpace(userimg))
                    {
                        <img src="~/dist/img/@userimg" class="img-circle" alt="Image" width="40px" style="border-radius:10em;" />
                    }
                    else
                    {
                        <img src="~/dist/img/AdminLTELogo.png" class="img-circle" alt="Image" width="40px" style="border-radius:10em;" />
                    }
                    @fullName
                </a> *@
            @* </div>
        </div> *@
        <br />
        <!-- Sidebar Menu -->
        <nav class="mt-2">
            <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
                <!-- Add icons to the links using the .nav-icon class
                with font-awesome or any other icon font library -->
                <li class="nav-item">
                    <a asp-controller="Home" asp-action="Index" class="nav-link @ViewBag.isdashboard">
                        <img src="~/dist/img/Dashboard Layout.png" alt="Image" width="30px"/>
                        <p>
                            Dashboard
                        </p>
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-controller="Home" asp-action="Role" class="nav-link @ViewBag.isrole">
                        <img src="~/dist/img/role.png" alt="Image" width="30px" />
                        <p>
                            Role
                        </p>
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-controller="User" asp-action="User" class="nav-link @ViewBag.isuser">
                        <img src="~/dist/img/User.png" alt="Image" width="30px" />
                        <p>
                            User
                        </p>
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-controller="Patient" asp-action="Patient" class="nav-link @ViewBag.ispatient">
                        <img src="~/dist/img/Patient.png" alt="Image" width="30px" />
                        <p>
                            Patient
                        </p>
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-controller="Appointment" asp-action="Appointment" class="nav-link @ViewBag.isappointment">
                        <img src="~/dist/img/medical-appointment.png" alt="Image" width="30px" />
                        <p>
                            Appointment
                        </p>
                    </a>
                </li>
            </ul>
        </nav>
        <!-- /.sidebar-menu -->
    </div>

    <!-- /.sidebar -->
</aside>
